<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hugang.dao.Flight_ticketsDao">
	<select id="queryFlight_ticketsById"
		resultType="com.hugang.entity.Flight_tickets">
		select * from Flight_tickets where flight_id = #{flightId}
	</select>
</mapper>